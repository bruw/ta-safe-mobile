import { makeStyles } from "@rneui/themed";

export const stylesDeviceRegistrationAttributesCard = makeStyles(() => ({
    container: {
        marginBottom: 50,
    },
    title: {
        fontSize: 16,
    }
}));