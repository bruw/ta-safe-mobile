import { makeStyles } from "@rneui/themed";

export const stylesDeviceAttributesCard = makeStyles(() => ({
    container: {
        marginBottom: 50,
    },
    title: {
        fontSize: 16,
    }
}));