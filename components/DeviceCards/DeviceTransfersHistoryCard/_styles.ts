import { makeStyles } from "@rneui/themed";

export const stylesDeviceTransfersHistoryCard = makeStyles(() => ({
    container: {
        marginBottom: 50
    },
    title: {
        fontSize: 16,
    }
}));