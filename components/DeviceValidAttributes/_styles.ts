import { makeStyles } from "@rneui/themed";

export const stylesDeviceValidAttributes = makeStyles((theme) => ({
    title: {
        fontSize: 16,
    },
    cardContainerStyle: {
        marginBottom: theme.spacing.xl,
    }
}));