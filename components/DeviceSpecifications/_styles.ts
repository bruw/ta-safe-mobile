import { makeStyles } from "@rneui/themed";

export const stylesDeviceSpecifications = makeStyles(() => ({
    container: {
        marginBottom: 50,
    },
    title: {
        fontSize: 16,
    }
}));