import { makeStyles } from "@rneui/themed";

export const stylesDeviceTransfersHistory = makeStyles(() => ({
    container: {
        marginBottom: 50
    },
    title: {
        fontSize: 16,
    }
}));