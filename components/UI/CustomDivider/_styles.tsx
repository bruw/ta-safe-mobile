import { makeStyles } from "@rneui/themed";

export const stylesCustomDivider = makeStyles((theme) => ({
    container: {
        marginTop: theme.spacing.lg,
        marginBottom: theme.spacing.lg,
    },
}));