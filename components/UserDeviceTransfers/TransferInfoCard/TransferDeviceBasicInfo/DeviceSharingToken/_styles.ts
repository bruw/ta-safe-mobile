import { makeStyles } from "@rneui/themed";

export const stylesDeviceSharingToken = makeStyles(() => ({
    clipBoardContainer: {
        flexDirection: 'row',
        alignItems: 'center'
    },
}));